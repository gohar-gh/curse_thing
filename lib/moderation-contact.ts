// TODO: replace with the operator's real moderation/takedown inbox before
// launch — this placeholder is used by both the "report this certificate"
// link and the Terms takedown clause, so update it in one place.
export const MODERATION_EMAIL = process.env.MODERATION_EMAIL ?? "registrar@example.com";
